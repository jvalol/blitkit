# Hugo's output, and the screenshots scripts/build copies in from ../media.
# Both are generated: media/ at the repo root is the one copy that is kept.
/public/
/static/media/
/resources/
/.hugo_build.lock
